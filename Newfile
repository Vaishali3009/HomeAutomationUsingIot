package com.rbs.bdd;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Basic context load test for the EspSimulatorEngine main Spring Boot application class.
 * This ensures that the application context loads successfully without throwing exceptions.
 */
@SpringBootTest
class EspSimulatorEngineTest {

    /**
     * This test validates that the Spring Boot application context loads without errors.
     */
    @Test
    void contextLoads() {
        // If the application context fails to start, this test will fail.
    }
}
